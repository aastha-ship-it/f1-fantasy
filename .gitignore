# OS
.DS_Store
Thumbs.db

# Ad-hoc screenshots the user drops into the repo root for reference
Screenshot*.png
Screenshot*.jpg
# Debug / one-off screenshots dropped at the repo root during dev — keep
# them local rather than committing huge binaries on every iteration.
/dashboard-*.png
/predict-*.png
/profile-portraits-*.png
/standings-*.png

# Editors
.vscode/
.idea/
*.swp

# Node / Bun
node_modules/
.pnp
.pnp.js
bun.lockb

# Next.js
.next/
out/
build/
dist/

# Env
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Design-handoff capture artifacts (per .claude/skills/design-handoff)
**/_capture.spec.ts
plans/designs/**/implementation.png

# Coverage
coverage/
*.lcov

# Supabase
supabase/.branches/
supabase/.temp/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# gstack (local, not checked in)
.gstack/

# Claude local (may contain hooks / settings.local.json)
.claude/settings.local.json

# Playwright MCP scratch dir (session-local, not checked in)
.playwright-mcp/

# Claude per-session state — do not commit. settings.json (project-level
# hooks shared by the team) lives separately and should be committed if it
# exists; everything else under .claude/ is local.
.claude/cache/
.claude/memory/
.claude/projects/
